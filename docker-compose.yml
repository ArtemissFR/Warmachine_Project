services:
  warmachine-portfolio:
    build:
      context: .
      network: host
    container_name: warmachine-dashboard
    network_mode: host
    volumes:
      - ./data:/usr/src/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always
